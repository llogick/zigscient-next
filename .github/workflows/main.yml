name: CI

on:
  push:
    branches:
      - dev
  pull_request:
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: mlugg/setup-zig@v2
        with:
          version: master

      - name: Run zig build test-fmt
        if: matrix.os == 'ubuntu-22.04'
        run: zig build test-fmt

      - name: Run zig build check test-ls
        run: zig build check test-ls --summary all --zig-lib-dir ./lib/
